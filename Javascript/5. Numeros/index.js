let alturaCm = 180;
let alturaM = 1.80;
let peso = 55.2;

let alturaRedon = alturaM.toFixed(1);
let pesoRedon = Math.floor(peso);

let valor;

if ((Number.MAX_VALUE + 1) === Number.MAX_VALUE) {
    valor = true;
}